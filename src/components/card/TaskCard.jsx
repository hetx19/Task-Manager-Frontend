import React from "react";

const TaskCard = ({
  title,
  description,
  priority,
  status,
  progress,
  createdAt,
  dueDate,
  assignedTo,
  attachmentCount,
  completedTodoCount,
  todoCheckList,
}) => {
  return <div>Task Card</div>;
};

export default TaskCard;
